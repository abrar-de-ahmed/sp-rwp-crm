[phases.setup]
nixPkgs = ['...', 'postgresql']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npx prisma db push --accept-data-loss 2>/dev/null || true', 'npm run build']

[start]
cmd = 'node .next/standalone/server.js'
